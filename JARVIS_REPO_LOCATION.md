# Jarvis repository location

Per request, the Jarvis project is now a **separate Git repository** and not part of this stock-trading-site repository.

- Path: `/workspace/Jarvis`
- Default branch: `main`
- Working branch created: `jarvis-init`

To work on Jarvis:

```bash
cd /workspace/Jarvis
git status
git branch
```
